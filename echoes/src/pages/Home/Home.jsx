import { GContainer, GText } from 'GComponents';
import './Home.css';
const Home = ({ className }) => {
  return (
    <GContainer className={className}>
      <GText>Home</GText>
    </GContainer>
  );
};

export default Home;
